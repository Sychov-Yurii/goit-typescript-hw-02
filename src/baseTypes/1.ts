// Перетворіть цей код на TypeScript, вказавши відповідні типи для всіх змінних.

export {};

let age: number = 50;
let name: string = 'Max';
let toggle: boolean = true;
let empty: null = null;
let notInitialize: any; // Якщо не хочете вказувати тип, можете використовувати `any`
let callback: (a: number) => number = (a) => { return 100 + a };

export {};
